version: '3.8'
services:
  rp-python:
    build:
      context: ./rp-python
      dockerfile: Dockerfile
    volumes:
      - ./rp-python/src:/app/src
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV=development
